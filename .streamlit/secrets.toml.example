# Streamlit secrets 예시 파일
# 실제 사용 시 이 파일을 secrets.toml로 복사하고 값을 채워 넣으세요.
# secrets.toml은 .gitignore에 포함되어 있으므로 Git에 올라가지 않습니다.

# 마인드로직 API 키 (2단계 이후 자연어 파싱 기능 구현 시 사용)
SM_MINDLOGIC = "your_mindlogic_api_key_here"
